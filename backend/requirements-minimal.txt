# Alternative installation approach if main requirements.txt fails

# Core dependencies first
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
numpy==1.24.4

# ML dependencies (install separately if needed)
# torch==2.1.0+cpu --index-url https://download.pytorch.org/whl/cpu
# transformers==4.35.2
# sentence-transformers==2.2.2
# faiss-cpu==1.7.4